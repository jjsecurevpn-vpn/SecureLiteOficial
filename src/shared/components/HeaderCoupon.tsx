// Deprecated: `HeaderCoupon` was consolidated into `AppHeader` + `CouponModal`.
// Keeping a no-op export to avoid accidental import breakage during refactors.
export const HeaderCoupon = () => null;