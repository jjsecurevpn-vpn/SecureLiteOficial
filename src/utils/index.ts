// Barrel exports para utilidades
export { formatBytes, compareVersions, toPingNumber, pingClass, formatProtocol } from './formatUtils';
export { loadCredentials, saveCredentials, loadAutoMode, saveAutoMode, isTermsAccepted, acceptTerms } from './storageUtils';
